window.Orders={all(){return DB.get("orders",[])},render(){let e=document.querySelector("#ordersPage");if(!e)return;if(!Auth.current()){e.innerHTML="<div class=order>Please <a href=login.html>login</a>.</div>";return}let a=this.all();e.innerHTML=a.length?a.map(o=>`<article class=order style="margin-bottom:12px"><b>${o.id}</b> <span class=badge>${o.status}</span><p>${new Date(o.date).toLocaleString()} · ₹${o.total.toLocaleString("en-IN")}</p><p>${o.items.map(i=>PRODUCTS.find(p=>p.id===i.id)?.name+" × "+i.qty).join(", ")}</p><small>${o.address}</small></article>`).join(""):"<div class=order>No orders yet.</div>"}};