window.Auth={current(){return DB.get("user",null)},signup(name,email,password){let u=DB.get("users",[]);if(u.some(x=>x.email===email))return false;let n={id:String(Date.now()),name,email,password};u.push(n);DB.set("users",u);DB.set("user",{id:n.id,name,email});return true},login(email,password){let u=DB.get("users",[]).find(x=>x.email===email&&x.password===password);if(!u)return false;DB.set("user",{id:u.id,name:u.name,email:u.email});return true},logout(){DB.remove("user");location.href="index.html"}};