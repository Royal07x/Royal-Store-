window.Wishlist={items(){return DB.get("wishlist",[])},toggle(id){let a=this.items(),i=a.indexOf(id);i>=0?a.splice(i,1):a.push(id);DB.set("wishlist",a);UI.toast(i>=0?"Removed from wishlist":"Saved to wishlist")},render(){let e=document.querySelector("#wishlistPage");if(!e)return;let p=this.items().map(id=>PRODUCTS.find(x=>x.id===id)).filter(Boolean);e.innerHTML=p.length?p.map(UI.card).join(""):"<div class=order>Wishlist is empty.</div>"}};