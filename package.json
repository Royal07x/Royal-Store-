{
  "name": "royal-store-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Royal Store V2 — full-stack ecommerce foundation.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node backend/src/server.js",
    "start": "node backend/src/server.js",
    "seed": "node database/seed/seed.js",
    "test": "node --import ./tests/setup.js --test",
    "test:watch": "node --import ./tests/setup.js --test --watch",
    "test:integration": "node tests/integration/smoke.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0"
  }
}
